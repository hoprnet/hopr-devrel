{
  "name": "hopr-stake",
  "version": "independent",
  "private": true,
  "scripts": {
    "dev": "yarn workspace frontend dev",
    "build": "yarn workspace frontend build",
    "start": "yarn workspace frontend start",
    "export": "yarn workspace frontend export",
    "clean": "yarn workspace frontend clean"
  },
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**"
    ]
  }
}
